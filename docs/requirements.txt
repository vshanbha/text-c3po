# Docs-site dependencies only (MkDocs Material). App deps stay in
# requirements.txt. CI installs this to build the Pages site.
# Pinned exact 2026-09-13 (verified latest on PyPI); re-pin deliberately.
mkdocs-material==9.7.7
