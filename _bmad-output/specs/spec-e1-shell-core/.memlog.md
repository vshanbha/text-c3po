---
topic: E1 Flet shell plus local text translation core
updated: 2026-09-09T12:21
---

- (assumption) Headless run produces stories.yaml despite skill interactive-only default, because blueprint step 7 and invocation explicitly require spec plus stories.yaml per epic
- (decision) E1 scope is FR-1 to FR-5 plus FR-14 5x5 gate slice; file/live pipelines excluded
- (assumption) flet pin 0.86.5 per spine stack; E1 kickoff re-pin check becomes first story acceptance
- (event) Self-validate pass 1 coherence: all caps carry intent plus success, constraints bend design, non-goals explicit, signals testable
- (event) Self-validate pass 2 preservation: PRD FR-1 to FR-5 plus FR-14 5x5 slice, blueprint E1 gate, spine AD-1 AD-2 AD-5 AD-6 AD-10 AD-11 AD-12 absorbed; wrapper ceremony dropped
- (event) Spec finalized with stories.yaml: 7 E1 stories ordered ending in 5x5 gate story with done_checkpoint
- (assumption) Story 4 dirty tree is expected: never-commit policy leaves stories 1-3 uncommitted, so clean-tree check is waived by design
- (assumption) Story 4 probe uses stdlib urllib with ~2s timeout: no new deps before E2 requests, inside CAP-2 5s budget
- (decision) Story 4 needs no services/ mediator: app.py entry wires runtimes probe, strip takes plain values, ui never imports runtimes
- (assumption) Story 5 first dispatch: working tree dirty from uncommitted stories 1-4 outputs; never-commit policy prevents clean tree, proceeding with in-worktree state as baseline
- (decision) Story 5 spec drafted: prefix-match lfm2.5 default rule in runtimes, verbatim picker in ui, app wires refresh; ready-for-dev pending gate check
- (decision) Story 5 review: 23 findings triaged 0 high 0 medium 6 low 17 false, all reject, zero patches; V-gap rejects reuse runner-less rationale from stories 1-4
- (decision) Story 5 finalization: commit step skipped per never-commit dispatch constraint, files remain uncommitted by design as in stories 1-4
- (assumption) Story 6 first dispatch: dirty tree is prior stories 1-5 uncommitted work plus never-commit constraint, proceeding without HALT
- (event) Story 6 verify: matrix 78/78 plus mid-session supplement 2/2, live lfm2.5 call returns all four fields
- (assumption) Story 7 resume: eval_harness.py already exists untracked from prior partial run; continuing to run harness and verify rather than recreating
- (assumption) Story 7 finalize-only resume: gate evidence trusted from research.md, skipping live harness per dispatch
- (decision) Skipping step-03 re-implement: harness + research.md evidence already on disk, static checks pass; proceeding to step-04 review
- (assumption) Review diff scoped to story-7 files (harness, services init, research gate) not full tree since e70869c which predates stories 1-6
- (assumption) Story 7 resume: in-review spec has empty triage log on disk, so re-running all four review layers via bmad-dev to recover complete evidence
- (decision) Review diff scoped to story-7 files only (eval_harness, services init, research gate) since baseline e70869c predates stories 1-6 already reviewed
- (assumption) Skipping live harness re-run per dispatch; static checks only plus stubbed run_matrix verification
- (assumption) Story 7 resume: reusing existing /tmp/story7-review-e1-gate.diff (17KB, scoped to services init + research gate + untracked eval_harness) as still fresh; no rebuild needed
- (decision) Review layers routed via bmad-dev subagents per dispatch constraint, not built-in reviewers; live harness run skipped per constraint
- (assumption) Retro headless: epic is stories-mode spec folder spec-e1-shell-core (invocation-named), stories.yaml ids 1-7 all status done so pending_stories empty
- (assumption) Retro: SSE-timeout/contributor-tier/__file__-doubling/orchestrator-timeout claims come from invocation context, not in-repo artifacts; sourced as orchestrator-reported except path-doubling and MLX-OOM which are evidenced in eval_harness.py docstring and pyproject.toml
- (decision) Retro verdict accepted-with-open-items on declared SPEC criteria; canonical doc at spec-folder RETROSPECTIVE.md plus mirror copy under implementation-artifacts per invocation
- (event) Retro behavior check: AST clean plus stubbed run_matrix 25/25 PASS via uv run with zero LLM calls; live LLM and native Mac eyeball not exercised this run
- (event) Retro complete: verdict accepted-with-open-items, 8 action items proposed, canonical RETROSPECTIVE.md plus implementation-artifacts mirror written
- (change) Operator follow-up: translation service pinned to num_ctx=4096 (single live lfm2.5 call 8s, all 4 fields correct); gate re-run not needed, E4 matrix records ctx per row
- (change) Operator restructure: package moved to src/text_c3po (src layout, valid identifier), tests to tests/, imports absolute, conftest removed for pythonpath=src; E1 specs keep old paths as finalized records
- (change) Operator UI polish: real AppBar with icon actions, compact toolbar, scrolling results, 8pt spacing; top_strip API is now build_appbar plus build_toolbar
- (change) Operator UI fix from screenshot: removed expand-plus-wrap conflicts (fixed 260px dropdown widths) and nested expanding scroll (body void); view root flexes, results scroll internally
- (change) Operator UI polish from screenshots: seeded Material theme, Card outputs, AppBar with icon actions, compact toolbar, page-level scroll, input max_lines 8
- (change) Operator UI fixes: ft.app to ft.run(main) positional; removed expand-plus-wrap conflicts (fixed 260px dropdowns); unnested expanding scrolls; STRETCH halves for side-by-side view
- (change) Operator translation changes: dropped commentary end-to-end (schema, prompt, tabs, render); paragraph chunking with per-chunk calls rejoined; UI sends names via languages.name_for_code; prompt pins entire-output-in-target-language after chunk 4-5 English drift observed live
- (change) Operator layout: side-by-side input plus Formal/Informal TabBar plus TabBarView (0.86 API); TabBarView pane fixed at 420px height with internally scrolling pages after unbounded-height error
- (event) Post-acceptance hardening commit 6abf0aa: streaming JSON transport (same single prompt/contract), single call per Translate (paragraph splitter removed), reasoning=False, num_ctx pin removed; Stop aborts server-side; shared paths helper; installable package; gate re-run lfm2.5 25/25 PASS recorded in research.md
- (learning) Speed 1 — thinking phase cost ~10s/call on lfm2.5 with zero translation gain; reasoning=False cut first token 5.5s to 0.7s and chunk flood 400 to 43
- (learning) Speed 2 — num_ctx=4096 premise wrong for this hardware: ollama ps shows lfm2.5 at 128K fully on GPU (7.6 GB); the pin also forced a model reload per call when it differed from loaded context; override removed, server default decides
- (learning) Speed 3 — paragraph splitter multiplied the per-call first-token wait N times on long pastes; single call translates cleanly, so split removed; char-based token guard deferred as unnecessary at 128K context
- (learning) Speed 4 — streaming overhead ~0.6s/call buys live progress plus true server-side Stop (stream close aborts mid-generation); LangChain token estimation rejected (needs transformers + per-model tokenizer for an approximate count)
- (change) Operator UI from screenshots: DeepL-like text view (language bar, equal cards, char count, centered actions); Formal/Informal tabs replaced by Material SegmentedButton toggle (no tab strip, single body flips visibility)
- (change) Operator UI from screenshot: Translate/Stop into language bar (button was below fold), cards 460 to 360, single output scroll region (first line hid behind header), AppBar icons replaced by labeled Retry/Models buttons, Translate auto-flips to Formal
- (change) Operator toolbar from feedback: AppBar Retry/Models removed (title-only); status dot/label is the retry control (click re-probes, hover shows server detail); model list refreshes on every probe; 15s poll plus Translate pre-probe fix stale green dot
- (change) Operator status row from feedback: sentence removed (width), dot-only with hover detail plus fix; down state M3 error red; disconnect announces via SnackBar with Retry
