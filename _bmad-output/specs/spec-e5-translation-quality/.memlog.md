---
topic: translation quality evals beyond contract conformance
updated: 2026-09-14T20:59
---

- (decision) E5 scope is meaning-equivalence evals only: FLORES+chrF++ deterministic quality numbers (CAP-1) plus COMETKIWI reference-free QE for arbitrary text (CAP-2); cross-model LLM judging explicitly deferred to Non-goals with revisit trigger
- (capability) CAP-1: operator can score any installed model on FLORES devtest references with sacrebleu chrF++ per language, recorded in research.md
- (capability) CAP-2: operator can score arbitrary source-hypothesis pairs with a local COMETKIWI model, no reference needed
- (constraint) Local-first holds: datasets and metric weights download once, then loopback-only; no cloud judge APIs ever
- (constraint) Serial single-request LLM discipline and manual-only execution (never CI) extend to all E5 runs; gate math and table shape untouched
- (assumption) FLORES-200 covers kn/mr/es/pt/hi/de under CC-BY-SA and sacrebleu plus COMETKIWI weights are pip/HF-installable — verify at build
- (question) FLORES devtest is 1012 sentences per language — what sample size per language keeps local Ollama runs tractable?
- (question) COMETKIWI needs torch — accept the heavy dep or prefer the deferred gemma-as-judge instead?
- (event) Self-validate pass 1 coherence: CAP-1/2 carry intent plus testable success, constraints rule out cloud judges/CI/parallelism, non-goals explicit, IDs fresh; bench names kept in intents as the requirement itself
- (event) Self-validate pass 2 preservation: all seven load-bearing session claims routed (scope ruling, model-vs-software split, hog override, judging deferral, FLORES plus KIWI recommendation, two open questions); spec finalized
- (decision) CAP-1 scores the formal contract field per language (formal is the primary output; informal register varies by target)
- (event) Self-validate re-pass on CAP-1 formal-field wording: intent now names the scored field, success semantics unchanged; no capability IDs touched
- (event) Prose-pass clarification of the pass-1 verdict line: bench names stay in CAP intents because the bench is the requirement being scored, not an implementation detail
- (decision) D14 per owner: Success signal names which artifact proves which half (capability survey for coverage, chrF++ table for quality)
