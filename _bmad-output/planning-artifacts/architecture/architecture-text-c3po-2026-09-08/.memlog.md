---
scope: text-c3po v2 local-first desktop translator: Flet shell, ChatOllama text core, sounddevice+VAD+whisper-server ASR, live captions UI, setup/eval/packaging
purpose: build-substrate
altitude: feature
updated: 2026-09-08T18:24
---

- (constraint) Blueprint 2 locked: Flet GUI pinned exact, ChatOllama via langchain-ollama runtime picker from /api/tags default lfm2.5, whisper-server subprocess 127.0.0.1:9001 transcribe-only, sounddevice capture with picker, split JSON contracts, zero-outbound loopback-only, all code in text-c3po/
- (version) flet==0.86.5 latest stable per pip index (research 2026-09-08); Flet 1.0.0.dev0 prerelease exists, do not track
- (version) ollama 0.33.3 live at 127.0.0.1:11434 with lfm2.5 5.2GB present; whisper-server ggml 0.23.0 at /opt/homebrew/bin/whisper-server, ggml-small.bin ready ~2s; python 3.12.10; sounddevice absent pre-E2 needs brew portaudio
- (decision) AD-1 paradigm layered modular monolith in one Flet process (ui/services/runtimes); prevents pipeline threads touching controls and UI importing ASR internals
- (decision) AD-2 dependency direction ui->services->runtimes->external processes, callbacks only upward; prevents import cycles between captions UI and ASR/LLM clients
- (decision) AD-3 Session single-owner of ordered Caption list, memory-only; pipeline threads post immutable utterance results via queue, only session controller mutates; prevents reorder/loss on mode switch
- (decision) AD-4 UI-thread rule: only main thread touches Flet controls; background capture/ASR/LLM threads use queue plus page callbacks; prevents races and Flet cross-thread crashes
- (decision) AD-5 split translation contract: text mode full Translation schema via JsonOutputParser, live/file lightweight {text,source_lang}; ChatOllama JSON format enforced; malformed JSON surfaces as retryable error never raw; prevents two modes inventing two parsers
- (decision) AD-6 model-agnostic backend: model is runtime string from /api/tags default lfm2.5-else-first; no model-specific branches; quality variance owned by eval harness never UI gating; prevents per-model code forks
- (decision) AD-7 ASR boundary transcribe-only: whisper-server HTTP on 127.0.0.1:9001, never -tr; translation owned exclusively by LLM; prevents two translators disagreeing per utterance
- (decision) AD-8 single ProcessManager owns whisper-server spawn/health/restart/cleanup; UI reads status only; prevents orphan processes and double-spawn on relaunch
- (decision) AD-9 capture boundary: sounddevice enumerates devices each launch no hardcoded names; VAD emits immutable Utterance WAV bytes; file mode bypasses capture via ffmpeg decode into same ASR entry; prevents device brittleness and capture/file pipeline fork
- (decision) AD-10 single LANGUAGES constant 23x{code,name} shared by UI pickers and whisper -l flag; backend never gates model x language combos; prevents picker/ASR drift
- (decision) AD-11 loopback-only networking post-download: 127.0.0.1:11434 and :9001 only; langchain-openai banned, no secrets files; prevents accidental outbound or key reintroduction
- (decision) AD-12 recoverable errors render inline in failing card/row plus SnackBar retry, never modal dialog; prevents inconsistent error UX across modes
- (decision) AD-13 setup and packaging owned by setup.sh (portaudio, model pull, BlackHole guidance) and bundle Info.plist NSMicrophoneUsageDescription; prevents TCC denial and setup drift
- (assumption) sounddevice 0.5.6 install dry-run from research; E2 confirms on install; flet re-pin check at E1 kickoff
- (question) PRD Q1: confirm flet==0.86.5 pin at E1 kickoff or re-pin newer stable with changelog note
- (question) PRD Q2/Q3: file-mode container floor (assume mp3/wav/m4a/mp4) and live source-lang default (assume auto-detect else English)
- (event) reviewer gate: lint clean after brace fix; adversary lens closed 3 holes (queue-drain owner AD-3/AD-4, whisper-restart gap labeling AD-8, harness model-as-parameter AD-6); version lens confirmed flet/ollama/whisper/python against research plus live env
- (event) spine finalized
- (decision) 2026-09-13 D3 approved (option B — build pump): AD-4 confinement upgraded from tolerated deviation to enforced main-thread render pump; E3-6 decoupling first, then pump implementation; spine AD-4 text updated on completion
